// const sum = (a, b) => a + b;
// export default sum; //export by default

// Named Export
export const sum = (a, b) => a + b; // named export

export const pow = (a, b) => a ** b; // named export

export const greet = (user) => `Hello ${user}`; // named export

// Default export
const products = (m, n) => m * n; //export by default
export default products;
